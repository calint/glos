{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "glos run",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "${workspaceFolder}/run.sh",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}