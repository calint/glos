{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "glos build",
            "type": "shell",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "${workspaceFolder}/build.sh",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}