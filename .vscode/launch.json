{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "glos: debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/glos",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "glos: build",
    }
  ]
}